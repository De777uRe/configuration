" Toggle number lines
set number

" Turn on syntax highlighting
syntax on

" Copy indent from the current line when starting new line
set autoindent

" Replace tabs with whitespace characters
set expandtab

" Set tab size
set shiftwidth=2

" A tab character indents to the 2nd column
set tabstop=2

" Auto Indenting
set ai

" Search highlighting
set hlsearch

" Incremental search
set incsearch

" Always show cursor position
set ruler

" If search contains upper, be case sensitive; if all lower, be case insensitive
set ignorecase
set smartcase